# CLAUDE.md — working in Dead Reckoning

How to work in this mod. Orientation lives in the doc set — read those, don't duplicate them here.

- **[README.md](README.md)** — human quick-start + status.
- **[STRUCTURE.md](STRUCTURE.md)** — where the code lives (component map).
- **[docs/ARCHITECTURE.md](docs/ARCHITECTURE.md)** — how the tracking system works.
- **[docs/DECISIONS.md](docs/DECISIONS.md) · [FEATURES.md](docs/FEATURES.md) ·
  [ROADMAP.md](docs/ROADMAP.md) · [BACKLOG.md](docs/BACKLOG.md) · [GOTCHAS.md](docs/GOTCHAS.md)**
- **[DESIGN.md](DESIGN.md)** — the original session-by-session design history (kept as a record;
  current state is in the doc set above).

## This mod is its own git repo

Dead Reckoning is a standalone git repo nested in the Moonlight Peaks workspace. When working here,
**this** repo is active — honor its own structure-review gate and baseline, not the root's. Scope
every sweep with `git ls-files --cached --others --exclude-standard`. Workspace conventions
(multi-repo rules, the release/pack pipeline) live in the root [../../CLAUDE.md](../../CLAUDE.md) and
[../../STRUCTURE.md](../../STRUCTURE.md).

## What this is

A BepInEx 5 / HarmonyX plugin (netstandard2.1) for the Unity Mono game *Moonlight Peaks*. It spawns
the in-game soul-blob "skull" and steers it toward a tracked NPC / place / map pin / quest. Released
as [Nexus mod 144](https://www.nexusmods.com/moonlightpeaks/mods/144), current v1.2.1.

## Conventions

- **Layout:** plugin `.cs` **flat in `src/`** (no `src/DeadReckoning/`); docs + `pack.ps1` at the
  mod root.
- **Versioning:** bump `<Version>` in `src/DeadReckoning.csproj` only, only when publishing. Never
  hardcode a version in `Plugin.cs` (`ModBuildInfo.Version` single-sources it).
- **Synced canonicals:** `pack.ps1` and `Directory.Build.props` are workspace-synced — do **not**
  edit them here; they're regenerated by `../../tools/sync-mod-files.ps1`.
- **Game integration is additive-only:** Harmony patches are Postfixes that add UI and call
  `SkullGuide.Instance`; never mutate the game's own flow.
- **Commit identity:** `dirtyredz <dirtyredz@live.com>`.
- **Never launch the game** — the user tests in-game. Never publish `dist/`, `bin/`, `obj/`, or
  decompiled game code.

## Build & release

- Build auto-deploys the DLL + `track-icon.png` to `BepInEx/plugins/MoonlightPeaksMods/DeadReckoning`.
- Pack: `powershell -File pack.ps1` → `dist/DeadReckoning-<version>.zip` (Nexus layout).
- Publish/update the Nexus page with the workspace **nexus-publish** skill. Full chain in the root
  [docs/ARCHITECTURE.md](../../docs/ARCHITECTURE.md).

## Structure-review gate

This repo is gated (pre-push hook, installed 2026-08-22). Edit/debug freely; the review fires once at
**push** on the accumulated change, not per edit or commit. Commit at logical boundaries; Claude runs
the review and pushes (asking first) when work is ready. `/gate status` shows what's pending. Biggest
known debt: `SkullGuide.cs` is a God-file — see [docs/BACKLOG.md](docs/BACKLOG.md) before adding to it.
